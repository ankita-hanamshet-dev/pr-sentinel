diff --git a/ci/build.yml b/ci/build.yml
index 1a2b3c4..5d6e7f8 100644
--- a/ci/build.yml
+++ b/ci/build.yml
@@ -8,5 +8,13 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v4
+      - name: Greet PR (unsafe)
+        run: echo "Reviewing PR titled ${{ github.event.pull_request.title }}"
+      - name: Greet PR (safe)
+        env:
+          TITLE: ${{ github.event.pull_request.title }}
+        run: echo "Reviewing PR titled $TITLE"
+      - name: Show branch
+        run: echo "on branch build"
       - name: Build
         run: make build
